{
  "name": "@ansh0108/keelcode",
  "version": "0.2.0",
  "bin": {
    "keel": "dist/bin.js"
  },
  "scripts": {
    "build": "node build.mjs && rm -rf ui-dist && cp -r ../ui/dist ui-dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist ui-dist",
    "prepublishOnly": "node build.mjs && rm -rf ui-dist && cp -r ../ui/dist ui-dist"
  },
  "files": [
    "dist",
    "ui-dist"
  ],
  "dependencies": {
    "better-sqlite3": "^12.9.0"
  },
  "devDependencies": {
    "@keelcode/analyzer": "workspace:*",
    "@keelcode/core": "workspace:*",
    "@keelcode/recorder": "workspace:*",
    "@keelcode/server": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "commander": "^12.0.0",
    "esbuild": "^0.28.0",
    "zod": "^3.25.76"
  }
}
