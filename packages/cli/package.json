{
  "name": "@keel/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "keel": "./dist/bin.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@keel/core": "workspace:*",
    "@keel/recorder": "workspace:*",
    "@keel/analyzer": "workspace:*",
    "@keel/server": "workspace:*",
    "commander": "^12.0.0"
  }
}
