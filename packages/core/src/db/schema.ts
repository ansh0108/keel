// Table name constants — single source of truth for raw SQL queries
export const TABLE = {
  sessions: 'sessions',
  executionNodes: 'execution_nodes',
  qualityMetrics: 'quality_metrics',
  branches: 'branches',
  constraints: 'constraints',
} as const
